[project]
name = "mantis"
revision = "1.0"

[profiles.default]
SESSION_SECRET = { description = "Session-cookie signing key for the isolated development app; at least 32 random characters. Do not reuse a production key.", required = true }

[scopes.development]
secrets = ["SESSION_SECRET"]
