[project]
name = "mantis"
revision = "1.0"

[profiles.default]
SESSION_SECRET = { description = "Session cookie signing key for the isolated development app; at least 32 random characters (application minimum: 16).", required = true }

[scopes.development]
secrets = ["SESSION_SECRET"]

[scopes.agent]
secrets = ["SESSION_SECRET"]
