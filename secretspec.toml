[project]
name = "mantis"
revision = "1.0"

[profiles.default]
SESSION_SECRET = { description = "Session signing and local token encryption secret; at least 16 characters after trimming. Reuse the securely stored input where available.", required = true }

[scopes.development]
secrets = ["SESSION_SECRET"]

[scopes.agent]
secrets = ["SESSION_SECRET"]
